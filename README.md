Tools for tuning the hyper-parameters that are based on scikit-learn, but have
different interface / output.  

As for sklearn GridSearchCV, to set parameters for a pipeline use format '<step_name>__<param_name>'